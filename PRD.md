# PRD — Early5 (얼리파이브)

> 개발용 실전 PRD · MVP 스코프 확정판 (v2.1 — **M1 구현 완료**)
> 원본 기획서(`PRD_지각방지_잠금화면시계.md`)의 검증 결과와 유저 인터뷰를 반영해 핵심 메커니즘을 재정의했다.
> **구현 상태**: M1 코어 루프(온보딩 → 약속 등록 → AI 자동 출발시각 → 카운트다운 → Safe! 판정 → 개인화 보정) 구현·검증 완료. 주요 코드: `src/lib/engine.ts`(개인화 엔진), `src/store/useStore.ts`(상태/영속), `src/app/{onboarding,new,countdown/[id]}/page.tsx`(화면).

---

## 1. 서비스 개요

| 항목 | 내용 |
|---|---|
| **서비스명** | Early5 (얼리파이브) |
| **한 줄 설명** | 입력만 하면 AI가 출발시각을 알아서 정해주는, "일찍 도착" 지각 방지 앱 |
| **대상 유저** | 캐주얼한 약속(친구·데이트)에 5~15분씩 습관적으로 늦는 사람 (특히 극P·ADHD 성향, "의지로는 안 고쳐지는" 지각러) |
| **핵심 문제** | 지도 앱의 예상 이동시간을 믿고 "딱 맞게" 계산하다 지각한다. 특히 대중교통 혼잡(만석 열차/버스 몇 대 보내기), 환승·택시 지연으로 지도 ETA는 늘 낙관적이다 (Phase 0: 지각 원인의 80%가 "이동시간을 딱 맞게 계산"한 실패) |
| **해결 컨셉** | 버퍼를 **준비가 아니라 이동/도착 쪽**에 둔다. 목표를 "정시 도착"이 아니라 **"N분 일찍 도착"**으로 바꾸고, 지도 ETA를 하한선으로만 취급해 넉넉히 패딩한 출발시각을 정한다. 그리고 이 계산을 **유저가 아니라 AI가 자동으로** 한다 |

### 왜 이 방향인가 (핵심 인사이트)

- **여유는 준비 쪽에 주면 먹힌다.** 준비 시간에 버퍼를 주거나 시계를 앞당겨도, 유저는 "시간 남았네" 하고 준비 task를 늘려(옷 한 번 더, 화장 다시) 그 여유를 소비해버린다 (파킨슨 법칙 + 계획 오류 + 시간맹). → **원본의 "가짜 시계 + 숨긴 랜덤버퍼" 전제는 폐기.**
- **여유를 이동 쪽에 주면 안 먹힌다.** "30분 일찍 도착"을 목표로 하면, 지하철에 일찍 앉아있는 시간은 task로 늘릴 수 없다. 유저 인터뷰에서 "약속시간보다 30분 일찍 도착 목표로 바꾸니 해소됐다"고 확인됨.
- **지도 ETA는 진실이 아니라 하한선.** 부산에선 맞던 계산이 서울에선 대중교통 혼잡/택시 지연으로 늘 틀어진다. 그래서 ETA에 리스크만큼 넉넉히 패딩해야 한다.
- **유저가 계산하면 안 된다.** "몇 분 일찍 나갈까"를 유저가 고민하는 순간 이미 실패다. AI가 패턴을 분석해 전부 자동 설정하고, 유저는 사실만 입력하고 확인만 한다.

### 핵심 기능 3가지

1. **사실만 입력 → AI가 출발시각 자동 설정** — 유저는 약속시각·도착지·이동수단·지도앱 예상시간만 넣는다. AI 개인화 엔진이 이동 패딩·도착 여유·출발시각을 전부 자동 산출한다. 유저는 확인만.
2. **정직한 카운트다운 + 신호등 넛지** — "출발까지 ○분"을 (가짜 없이) 실시간 표시. 출발시각까지 남은 시간에 따라 초록→노랑→빨강으로 긴박감을 시각화. 출발 순간 단 하나의 강한 알림.
3. **도착 기록 → 개인화 보정 루프** — 도착 시 "Safe!" 한 번 탭으로 실제 도착시각 기록. 목표 대비 얼마나 일렀/늦었는지로 다음 버퍼를 자동 보정 → 쓸수록 정확해진다.

### 사용 기술 (확정)

| 레이어 | 선택 | 이유 |
|---|---|---|
| 프레임워크 | **Next.js 16 (App Router) + TypeScript** | 빠른 스캐폴딩, Vercel 배포 즉시성 |
| 스타일 | **Tailwind CSS** | 토스풍 미니멀 디자인을 빠르게 구현 |
| UI 컴포넌트 | **shadcn/ui** (필요한 것만 선별) | 접근성·일관성, 커스터마이즈 용이 |
| 상태/저장 | **Zustand + localStorage** (백엔드/DB 없음) | 서버·로그인 없이 단일 기기 완결 |
| **개인화 엔진** | **규칙/가중치 기반 로컬 엔진** (LLM·API 키 없음) | 즉각·무료·오프라인·테스트 용이. MVP 검증에 충분 |
| 알림 | **Web Notifications + Vibration API** + 상시 카운트다운 | 강한 단일 알림, 일괄 해제 방지 설계 |
| 이동시간 | **MVP: 유저가 지도앱 예상시간 입력 → 엔진이 패딩** | 지도 API 키 없이 착수. 지도 API 실연동은 후속 |
| 배포 | **Vercel** | — |

---

## 2. 디자인 톤 & 원칙 (토스풍)

- **테크·미니멀·세련·극도의 직관성**, "문제 해결"에만 집중.
- **컬러:** 무채색 베이스(화이트/그레이) + 강조 1색(딥 블루). 신호등 상태색(그린/앰버/레드)은 카운트다운에서만 강하게.
- **타이포:** 큰 숫자 중심(카운트다운). 위계는 굵기·크기·여백으로만.
- **인터랙션:** 버튼 하나로 끝나는 흐름. 유저가 숫자·설정을 만질 일이 없게. 계산식·버퍼 수치는 감춘다.
- **다크모드 대응**, **모바일 퍼스트**(세로 우선, 데스크톱은 중앙 카드).

---

## 3. 개인화 엔진 (AI 자동 산출 — 유저 설정 없음)

유저는 아래 값을 **직접 고르지 않는다.** 온보딩 패턴 + 약속 맥락 + 누적 도착 이력을 입력으로 엔진이 전부 산출한다.

```
입력(유저): 약속시각, 도착지, 이동수단, 지도앱 예상시간,
           (온보딩) 평소 습관·늦는 이유·지각 빈도/폭
        ↓  규칙 기반 개인화 엔진
산출(자동): 이동 패딩(분), 도착 여유(분), 출발시각, 준비 시작 시각
```

- **이동 패딩**: 이동수단(도보/대중교통/자차·택시)별 기본 리스크 + 유저의 과거 "실제 소요 vs 지도 ETA" 편차로 산출. 대중교통일수록 크게(혼잡·환승·배차 변동).
- **도착 여유**: 유저의 지각 성향(빈도/폭)으로 산출. 더 잘 늦는 유저일수록 크게.
- `출발시각 = 약속시각 − 도착여유 − (지도ETA + 이동패딩)`
- **가짜 시계 없음.** 카운트다운은 실제 출발시각까지의 정직한 남은 시간을 표시.
- UI는 "○시에 출발하세요 → ○시 도착 목표"만 보여주고 계산식·버퍼 수치는 감춘다.
- **보정 루프**: Safe! 시 실제 도착이 목표보다 크게 이르면 다음 버퍼↓, 지각하면↑.

---

## 4. 핵심 사용자 흐름

```
[온보딩] (최초 1회)
  평소 준비 습관 / 자주 늦는 이유 / 지각 빈도·폭 응답
  → AI 초기 개인화 프로필 생성 (유저는 '분'을 고르지 않음)
        ↓
[1] 약속 등록 (사실만 입력, 30초)
  · 약속 일시
  · 도착지
  · 이동 수단 (도보/대중교통/자차·택시)
  · 지도앱 예상 이동시간
        ↓
[2] AI 자동 계산 (즉시)
  이동 패딩 + 도착 여유를 자동 반영해
  출발시각 = 약속시각 − 도착여유 − (지도ETA + 이동패딩)
        ↓
[3] 확인 화면
  "○시 ○분에 출발하세요 → ○시 도착 목표" 확인 → 저장
  (계산식·버퍼는 숨김. 조정 필요 없음)
        ↓
[4] 출발 시각 도달
  · 강한 단일 알림 + 진동 (일괄 해제 불가)
  · 카운트다운 화면: "출발까지 ○분" 정직하게 실시간 표시
  · 신호등: 여유(초록) → 슬슬 준비(노랑) → 지금 나가세요(빨강)
        ↓
[5] 도착 → "Safe!" 탭
  · 카운트다운 종료
  · 실제 도착시각 기록 → 조기/정시/지각 판정
        ↓
[6] 개인화 보정
  목표 대비 편차로 다음 이동 패딩·도착 여유 자동 조정
```

---

## 5. 화면 목록 (라우팅 · M1 범위)

| 경로 | 화면 | 핵심 요소 |
|---|---|---|
| `/onboarding` | 온보딩 | 습관·늦는 이유·지각 빈도/폭 응답 → 초기 프로필 생성 |
| `/` | 홈 | 다가오는 약속 카드("○시 출발 → ○시 도착 목표"), [+ 약속 추가] |
| `/new` | 약속 등록 | 일시·도착지·이동수단·지도ETA 입력 → AI 산출 결과 확인 |
| `/countdown/[id]` | 카운트다운 | "출발까지 ○분" 대형 표시, 신호등 배경, [출발했어요]/[Safe! 도착] |

> `/history`(대시보드·스트릭), `/settings`, 준비 러닝시트, 중요한 날 전용 처리는 후속(M2).

---

## 6. 기능 요구사항 (인수 기준)

### FR-1. 온보딩 & 초기 프로필
- **Given** 최초 진입, **When** 습관·늦는 이유·지각 빈도/폭을 응답하면, **Then** 초기 개인화 프로필이 localStorage에 저장되고 홈으로 이동한다.
- 유저는 '몇 분' 같은 수치를 직접 고르지 않는다. 성향만 답한다.

### FR-2. 약속 등록 (사실만)
- **Given** `/new`, **When** 일시·도착지·이동수단·지도ETA를 입력·저장하면, **Then** 약속이 생성되고 AI가 출발시각을 자동 산출해 확인 화면에 표시한다.

### FR-3. AI 자동 출발시각 산출
- 엔진이 `출발시각 = 약속시각 − 도착여유 − (지도ETA + 이동패딩)`을 계산한다.
- 도착여유·이동패딩은 온보딩 프로필 + 이동수단 + 누적 이력에서 자동 도출. **UI에 수치·계산식 미노출.**
- 출발시각이 현재보다 과거가 되지 않도록, 불가한 경우 "지금 바로 출발" 상태로 처리.

### FR-4. 카운트다운 (정직) & 강한 단일 알림
- **When** 출발시각 도달, **Then** 강한 단일 알림 + 진동이 발생하고 카운트다운 화면으로 유도한다.
- 표시되는 남은 시간 = **실제 출발시각까지의 정직한 남은 시간** (가짜 없음).
- 백그라운드→포그라운드 복귀 시 `Date.now()` 기준 재계산으로 어긋남 방지.

### FR-5. 신호등 넛지
- 카운트다운 창(예: 출발 60분 전 시작)~출발시각 기준 잔여 비율로: **>50% 초록 / 20~50% 노랑 / <20% 빨강**.
- 색상 전환 시 문구도 전환("아직 여유 있어요" / "슬슬 준비하세요" / "지금 나가야 해요").

### FR-6. Safe! 종료 & 판정
- **When** [Safe! 도착] 탭, **Then** 카운트다운 종료, 실제 도착시각 = 현재시각 기록.
- 판정: 도착 ≤ 약속시각−도착여유 → 조기(성공), ≤ 약속시각 → 정시, 초과 → 지각(초과 분 기록).

### FR-7. 개인화 보정
- 판정 결과를 프로필에 반영: 지각/빠듯이면 다음 이동 패딩·도착 여유↑, 크게 이르면 소폭↓ (규칙 기반).

---

## 7. 데이터 모델 (localStorage, MVP)

```ts
type TravelMode = 'walk' | 'transit' | 'car';

type UserProfile = {
  lateReasons: string[];         // 자주 늦는 이유 태그
  lateFrequency: 'rare' | 'sometimes' | 'often'; // 지각 빈도
  lateSeverity: 'minor' | 'moderate' | 'severe';  // 평소 지각 폭
  // 엔진이 학습·보정하는 내부 값 (유저 비노출)
  modeEtaBiasMin: Record<TravelMode, number>;     // 이동수단별 ETA 편차 보정
  arriveEarlyBaseMin: number;                     // 기본 도착 여유(엔진 관리)
};

type Appointment = {
  id: string;
  title?: string;
  destination: string;
  appointmentAt: number;         // 약속 시각 (epoch ms)
  travelMode: TravelMode;
  mapEtaMin: number;             // 유저가 입력한 지도앱 예상시간
  // 엔진 산출값 (내부)
  paddingMin: number;
  arriveEarlyMin: number;
  departAt: number;              // 출발시각 (epoch ms)
  status: 'scheduled' | 'active' | 'done';
  arrivedAt?: number;            // Safe! 시각
  outcome?: 'early' | 'ontime' | 'late';
  lateByMin?: number;
};
```

---

## 8. 성공 지표 (MVP)

**선행 지표**
- 약속 등록 완료율(폼 시작 대비 저장): ≥ 80%
- 카운트다운 화면 도달률: ≥ 70%
- Safe! 처리율: ≥ 85%

**후행 지표 (2~4주)**
- 자가 보고 지각률: 첫 4주 30%↑ 감소
- W2 리텐션: ≥ 40%
- "출발 시각이 적절했다" 긍정 응답: ≥ 70% (AI 산출 실효성 검증)

---

## 9. 마일스톤

| 단계 | 범위 |
|---|---|
| **M1 — 코어 루프 (✅ 완료)** | 온보딩(패턴) → 약속 등록(사실만) → AI 자동 출발시각 → 정직한 카운트다운(신호등) → Safe! 판정 → 보정. localStorage. |
| **M2 — 리텐션 & 정교화** | 히스토리 대시보드·스트릭, 준비 러닝시트(준비 구간 팽창 억제), 중요한 날(면접·소개팅) 전용 처리(전날 밤 준비 오프로드·도착여유 상향). |
| **M3 — 알림 & PWA** | 강한 웹 알림/진동 정교화, PWA 설치, 다크모드 마감. |
| **M4 — 지도 연동 & 계측** | 지도 API 실연동, 실측 기반 패딩 학습 고도화, 이벤트 로깅, 베타 코호트. |

---

## 10. 열린 결정 (개발 중 확정)

1. **패딩 기본값 세트**: 이동수단별 초기 패딩(대중교통/자차·택시/도보)의 시작값 — 넉넉하게 시작해 보정 루프로 수렴.
2. **카운트다운 창 시작 시점**: 출발 몇 분 전부터 화면·알림을 띄울지(예: 60분 전).
3. **웹 알림 신뢰성**: 브라우저/OS 백그라운드 제약 → 상시 카운트다운을 1차 신뢰 경로, 웹 알림은 보조.
4. **지도 API 연동 시점**: M4에서 유저 입력 ETA를 실연동으로 대체.
```