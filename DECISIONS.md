# Early5 — 설계 판단 기록 (Decision Log)

> AI로 "만들 수 있느냐"는 이제 기본값이다. 프로토타입, 깃허브 링크, 작동하는 화면까지는 다들 간다.
> 남는 건 판단이다 — **어디까지 직접 하고, 어디서 멈췄고, 왜 그게 이 문제에 맞는 선택이었는지.**
> 이 문서는 Early5를 만들며 내린 결정과 그 생각의 과정을 기록한다. 결과물이 아니라 *선택*을 남긴다.

각 항목은 이렇게 적는다:
- **결정 / 멈춘 지점** — 무엇을 하기로 했고, 어디서 손을 뗐나
- **왜 이 문제에 맞았나** — 이 선택이 *이 문제*의 실패 지점을 겨냥한 이유
- **버린 대안 · 트레이드오프** — 무엇을 포기했나

---

## 1. 무엇을 만들 것인가 — "웹 MVP"에서 멈춘 이유

**결정 / 멈춘 지점.** 네이티브 잠금화면 앱이 아니라 웹(Next.js/PWA)으로 시작했다. iOS Live Activity / Android Full-Screen Intent 같은 "진짜 잠금화면 시계"는 **의도적으로 손대지 않았다.**

**왜 이 문제에 맞았나.** 이 제품의 핵심 가설은 "이동 버퍼를 넉넉히 잡고 *일찍 도착*을 목표로 두면 지각이 준다"이다. 이 가설은 잠금화면 없이도 검증할 수 있다. 반대로 네이티브 잠금화면은 OS 정책상 웹으로 대체 불가능하고, 개발자 계정·스토어 배포·네이티브 모듈까지 비용이 가장 큰 부분이다. **검증되지 않은 가설에 가장 비싼 부분을 먼저 쓰지 않는다** — PRD 자체도 이를 Phase 2로 미뤄 두었다.

**버린 대안 · 트레이드오프.** "잠금화면에 강제로 뜨는 시계"라는 원안의 임팩트를 지금 100% 재현하지는 못한다. 대신 개념·흐름·계산 로직을 싸게, 빠르게 검증한다.

---

## 2. 핵심 메커니즘을 갈아엎은 판단 — "가짜 시계"를 버린 이유

**결정 / 멈춘 지점.** 원안은 *앞당겨진 가짜 시계*를 노출해 "지금 늦었다"는 착각을 만드는 것이었다. 이걸 핵심에서 내렸다. 대신 **"준비 구간을 실시간으로 통제한다 — 여유를 애초에 만들지 않는다"** 로 메커니즘을 재정의했다. 준비 시각이 되면 카운트다운이 "출발까지"로 바뀌고 "지금 준비 시작하세요"로 촉박하게 몰아붙인다.

**왜 이 문제에 맞았나.** 사용자가 반례를 줬다: *시계를 당겨도, 유저는 "여유 생겼네" 하고 준비 시간을 그만큼 늘려 상쇄한다*(계획 오류 + 파킨슨 법칙). 심지어 중요한 날일수록 준비 결정(뭘 입지 등)이 늘어 더 늦는다. 즉 진짜 실패 지점은 "출발 시각을 몰라서"가 아니라 **"준비 구간에서 여유가 팽창해서"** 다. 표면(시계 숫자)이 아니라 실패의 *원인*(준비 팽창)을 직접 겨냥해야 했다. 그래서 준비 시각을 산출해 보여주고, 그 시각이 오면 여유가 아니라 마감으로 제시한다.

**버린 대안 · 트레이드오프.** "체감 시간 조작"이라는 원래의 후크를 약화시켰다. 대신 요구를 그대로 코딩하는 대신, 반례를 받아들여 메커니즘 자체를 바꿨다. (요구사항이 아니라 문제를 푼다.)

---

## 3. "AI 분석"을 정직하게 그은 선

**결정 / 멈춘 지점.** 개인화 엔진은 **규칙 기반(온디바이스)** 으로 만들었다. ML이나 실시간 교통 예측인 *척하지 않았다.* 현재는 수동 ETA + 이동수단 리스크 + 지각 사유 + 중요도 + 약속별 위험요인으로 버퍼를 산출한다.

**왜 이 문제에 맞았나.** "AI"라는 포장 대신, 지금 당장 진짜로 유용하고 *설명 가능한* 계산을 먼저 세웠다. 실제 교통 데이터는 API 키·백엔드라는 외부 의존이 있어야 한다. 그래서 계산의 입력 경계(`computePlan`)를 깨끗하게 두어, **나중에 수동 ETA를 실시간 교통시간으로 갈아끼우기만 하면 되게** 설계했다. 지금 없는 걸 있는 척하지 않고, 대신 갈아끼울 자리를 만들어 뒀다.

**버린 대안 · 트레이드오프.** 지금의 예측 정확도는 유저가 넣는 ETA 품질에 의존한다. 대신 키·비용 없이 동작하고, 도착 결과로 프로필을 보정하는 학습 루프는 이미 돌고 있다.

---

## 4. 어디서 멈췄나 — 교통 API / 지도 장소검색

**결정 / 멈춘 지점.** 여기서 **의도적으로 멈췄다.** env 자리(`.env.local.example`)와 핸드오프 문서까지만 만들고, 실제 통합 코드는 짜지 않았다.

**왜 이 문제에 맞았나.** 이건 외부 API 키가 있어야만 진짜로 동작하고, 키는 유저가 발급해야 한다(유료·제공자 선택 필요). 키 없이 블라인드로 짜면 *테스트할 수 없는 취약한 코드*가 된다. "돌아가는 것처럼 보이는" 미검증 통합이나 가짜 장소검색/목데이터로 데모를 꾸미는 대신, **진짜 막힌 지점을 인정하고 깨끗하게 넘기는 쪽**을 택했다.

**버린 대안 · 트레이드오프.** 지금 당장 "지도 검색 되는 화면"을 보여주지 못한다. 대신 나중에 붙일 때 흔들리지 않는다.

---

## 5. 정직한 한계 — "웹으로는 잠금화면 시계가 안 된다"고 말한 것

**결정.** 웹으로 잠금화면 시계가 되는 척 포장하지 않고, OS 경계를 명확히 설명했다. 할 수 있는 것(설치형 PWA 알림)과 없는 것(상시 앞당겨진 시계)을 구분해 전달했다.

**왜 이 문제에 맞았나.** 과장은 반드시 나중에 신뢰를 깎는다. 특히 "잠금화면"은 이 제품의 심장이라, 여기서 얼버무리면 로드맵 전체가 어긋난다. 못 하는 건 못 한다고 먼저 말했다.

---

## 6. 디자인·UX 판단들 — 작지만 이유가 있는 것들

- **파스텔 배경을 걷어내고 강조에만 메인컬러.** 처음엔 옅은 브랜드색(`brand-weak`)을 카드마다 채웠다. 피드백 후 "꼭 강조할 것에만 색"이라는 토스식 원칙으로 되돌렸다. → *색이 흔해지면 위계가 사라진다.* 주요 시각·CTA·선택 상태에만 색을 남겼다.
- **중요도 = 배경 채움 대신 텍스트 색.** "중요도를 색으로 구분"과 "파스텔 배경 금지"라는 두 요구가 충돌했다. 채운 배지 대신 **색 텍스트**(중요=앰버, 매우 중요=레드)로 둘을 동시에 만족시켰다.
- **캘린더 월 선택.** 처음엔 제목을 누르면 피커가 아래로 펼쳐지며 헤더 화살표와 두 줄로 겹쳤다. → 피커가 열리면 **헤더 화살표를 "연도 이동"으로 전환**해, 화살표 한 줄만 유지하고 겹침을 구조로 없앴다.
- **하단 네비 = 플로팅 pill.** 가장자리에 붙은 바에서 레퍼런스처럼 떠 있는 알약으로. 스크롤 시 네비가 내용과 같이 밀리던 문제는 `transform`으로 fixed를 가두는 트릭의 함정이었고, **스크롤 영역과 고정 네비를 분리**해 되돌렸다.
- **아이폰 목업 프레임(데스크톱 전용).** 컴퓨터에선 실기기 감각으로 프레임 안에 렌더, 실제 폰에선 풀스크린(반응형). "작업하면서도 컴퓨터로 봐야 한다"는 요구에 맞춰 둘 다 되게 했다.
- **"기록"이라는 단어 제거.** "스포츠 같다"는 지적에 "일정/히스토리"로 교체. 단어 하나의 톤도 제품 인상이다.
- **프리셋 버튼 제거.** 소요시간을 "시간/분"으로 나눠 입력하게 되자 20·30분 프리셋은 중복이 됐다. → 기능이 겹치면 덜어낸다.
- **입력 최소화.** 준비 시간을 유저에게 직접 묻지 않고 사유·중요도·위험요인에서 산출한다. 유저는 "사실"만 넣고, 계산은 앱이 한다(30초 등록 원칙).
- **다가오는 약속 = 7일 이내.** 홈은 *행동 가능한 근미래*에 집중시키고, 먼 약속은 캘린더가 담당한다. 기준 없이 전부 쌓으면 홈이 의미를 잃는다.

---

## 7. 반복해서 작동한 원칙 (요약)

1. **요구를 그대로 받지 않는다.** "이게 진짜 문제를 푸는가"를 먼저 묻는다 (가짜 시계 → 준비 구간 통제).
2. **가장 비싼 부분은 미룬다.** 네이티브·교통 API는 검증과 의존이 준비될 때까지 손대지 않는다.
3. **못 하는 건 못 한다고 말한다.** 잠금화면·AI를 과장하지 않는다.
4. **막힌 지점은 숨기지 않고 깨끗이 핸드오프한다.** 가짜 데모로 메우지 않는다.
5. **색·단어·중복 UI까지 위계와 의도로 정리한다.** 강조는 아껴야 강조가 된다.

> 정리하면: Early5에서 코드를 직접 짠 건 "AI로 이런 것도 된다"를 보이려는 게 아니라, *이 문제*(준비 구간의 시간 팽창)를 풀기 위해 어디까지 만들고 어디서 멈출지를 매번 판단했기 때문이다. 이 판단의 흔적을 지우지 않는 것이 이 문서의 목적이다.
